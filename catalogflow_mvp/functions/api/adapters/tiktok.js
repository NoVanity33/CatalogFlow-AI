export async function publishTikTok({product,listing},env){if(!env.TIKTOK_APP_KEY||!env.TIKTOK_APP_SECRET||!env.TIKTOK_ACCESS_TOKEN||!env.TIKTOK_SHOP_ID)throw new Error('TikTok Shop Partner Center credentials and seller authorization are required.');return{status:'payload-ready',note:'Next milestone: category attributes, warehouse, shipping template, images, and signed Create Product request.',payload:{title:listing.title,description:listing.description,price:product.price,colors:product.colors,sizes:product.sizes,save_mode:'AS_DRAFT'}}}