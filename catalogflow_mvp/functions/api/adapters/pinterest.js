export async function publishPinterest({product,listing},env){if(!env.PINTEREST_ACCESS_TOKEN||!env.PINTEREST_CATALOG_ID)throw new Error('Configure Pinterest access token and catalog ID.');if(!product.image||!product.productUrl)throw new Error('Pinterest requires a public image URL and product URL.');const body={country:'US',language:'en-US',items:[{item_id:product.id,operation:'UPSERT',attributes:{title:listing.title,description:listing.description,link:product.productUrl,image_link:product.image,availability:'in stock',price:`${Number(product.price).toFixed(2)} USD`,condition:'new',brand:'No Vanity 33'}}]};const r=await fetch('https://api.pinterest.com/v5/catalogs/items/batch',{method:'POST',headers:{Authorization:`Bearer ${env.PINTEREST_ACCESS_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.message||'Pinterest update failed.');return{status:'submitted',batchId:d.batch_id||null,response:d}}