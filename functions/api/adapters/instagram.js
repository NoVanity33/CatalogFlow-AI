export async function publishInstagram({product,listing},env){if(!env.META_ACCESS_TOKEN||!env.META_CATALOG_ID)throw new Error('Meta access token and commerce catalog ID are required.');return{status:'payload-ready',note:'Next milestone: approved Meta app permissions and live catalog field mapping.',payload:{retailer_id:product.id,name:listing.title,description:listing.description,price:Math.round(Number(product.price)*100),currency:'USD',availability:'in stock',url:product.productUrl,image_url:product.image,brand:'No Vanity 33'}}}